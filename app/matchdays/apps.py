from django.apps import AppConfig


class MatchdaysConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "app.matchdays"
    label = "matchdays"
    verbose_name = "Matchdays"
